#include<bits/stdc++.h>
using namespace std;
using ll=long long;
/*
https://codeforces.com/contest/1872/problem/G

Codeforces Round 895 (Div. 3) G. Replace With Product 

Given an array a of n positive integers. You need to perform the following 
operation exactly once:

 Choose 2 integers l and r (1 \le l \le r \le n) and replace the subarray 
a[l \ldots r] with the single element: the product of all elements in the 
subarray (a_l \cdot \ldots \cdot a_r).



For example, if an operation with parameters l = 2, r = 4 is applied to 
the array [5, 4, 3, 2, 1], the array will turn into [5, 24, 1].

Your task is to maximize the sum of the array after applying this 
operation. Find the optimal subarray to apply this operation.
*/
void run(){
    // Welcome, your majesty.
}
int main(){
#ifdef WINE
    freopen("data.in","r",stdin);
#endif
    int T;scanf("%d",&T);
    while(T--){
        run();
    }
}
