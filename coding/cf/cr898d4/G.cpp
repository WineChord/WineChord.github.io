#include<bits/stdc++.h>
using namespace std;
using ll=long long;
/*
https://codeforces.com/contest/1873/problem/G

Codeforces Round 898 (Div. 4) G. ABBC or BACB 

You are given a string s made up of characters \texttt{A} and \texttt{B}. 
Initially you have no coins. You can perform two types of operations: 

 Pick a substring^\dagger \texttt{AB}, change it to \texttt{BC}, and get a 
coin. 

 Pick a substring^\dagger \texttt{BA}, change it to \texttt{CB}, and get a 
coin. 



 What is the most number of coins you can obtain?^\dagger A substring of 
length 2 is a sequence of two adjacent characters of a string.
*/
void run(){
    // Welcome, your majesty.
}
int main(){
#ifdef WINE
    freopen("data.in","r",stdin);
#endif
    int T;scanf("%d",&T);
    while(T--){
        run();
    }
}
