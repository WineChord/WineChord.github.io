#include<bits/stdc++.h>
using namespace std;
using ll=long long;
void run(){
    ll x,y;scanf("%lld%lld",&x,&y);
    if((x-1)%4==0&&y==0){
        puts("1");return;
    }
}
int main(){
    int T;scanf("%d",&T);
    while(T--)run();
}